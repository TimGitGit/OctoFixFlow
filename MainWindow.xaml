<Window
    x:Class="OctoFixFlow.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:h="clr-namespace:HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
    xmlns:local="clr-namespace:OctoFixFlow"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:OctoFixFlow.Resource"
    Title="{Binding Source={x:Static local:ResourceHelper.Instance}, Path=WindowTitle}"
    Width="700"
    Height="450"
    AllowsTransparency="True"
    Background="Transparent"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">

    <Window.Resources>
        <!--  阴影效果  -->
        <DropShadowEffect
            x:Key="DropShadowEffect"
            BlurRadius="15"
            ShadowDepth="5"
            Color="#40000000" />

        <!--  缓动函数  -->
        <CubicEase x:Key="CubicEaseOut" EasingMode="EaseOut" />

        <!--  输入框样式（带动画）  -->
        <Style x:Key="InputTextBoxStyle" TargetType="{x:Type Control}">
            <Setter Property="Background" Value="#F9F9F9" />
            <Setter Property="BorderBrush" Value="#E0E0E0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Height" Value="40" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="9" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Control}">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <!--  边框颜色动画（不变）  -->
                                            <ColorAnimation
                                                Storyboard.TargetName="border"
                                                Storyboard.TargetProperty="BorderBrush.Color"
                                                To="#FA4616"
                                                Duration="0:0:0.2" />
                                            <!--  边框厚度动画（替换为ThicknessAnimation）  -->
                                            <ThicknessAnimation
                                                Storyboard.TargetName="border"
                                                Storyboard.TargetProperty="BorderThickness"
                                                From="1"
                                                To="1.5"
                                                Duration="0:0:0.2" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                            <!--  鼠标悬停效果（不变）  -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#CCCCCC" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  按钮样式  -->
        <Style x:Key="AccentButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#FA4616" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Width" Value="100" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="5"
                            SnapsToDevicePixels="True">
                            <ContentPresenter
                                x:Name="contentPresenter"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Content}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <!--  鼠标悬停效果  -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D93E12" />
                            </Trigger>
                            <!--  点击效果  -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C23710" />
                                <Setter TargetName="contentPresenter" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <!--  禁用状态  -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC" />
                                <Setter TargetName="contentPresenter" Property="Opacity" Value="0.7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  退出按钮样式  -->
        <Style
            x:Key="ExitButtonStyle"
            BasedOn="{StaticResource AccentButtonStyle}"
            TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#666666" />
            <Setter Property="Margin" Value="10,0,0,0" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#555555" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#444444" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!--  主容器  -->
    <Border
        x:Name="LoginPanel"
        Background="White"
        CornerRadius="10"
        Effect="{StaticResource DropShadowEffect}">
        <!--  添加渲染变换用于动画  -->
        <Border.RenderTransform>
            <TranslateTransform />
        </Border.RenderTransform>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <!--  ShowFrameRate="True"  -->

            <Border
                Grid.Column="0"
                Background="#f5f5f5"
                CornerRadius="10,0,0,10">
                <h:HelixViewport3D x:Name="modelViewport" ZoomExtentsWhenLoaded="True">
                    <h:DefaultLights />
                    <ModelVisual3D x:Name="modelVisual" />
                </h:HelixViewport3D>
            </Border>

            <Rectangle
                Grid.Column="1"
                Width="1"
                Margin="0,30"
                VerticalAlignment="Stretch"
                Fill="Red" />
            <!--  右侧：登录表单  -->
            <Grid Grid.Column="2" Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  标题  -->
                <StackPanel
                    Grid.Row="0"
                    Margin="0,30,0,25"
                    HorizontalAlignment="Center">
                    <TextBlock
                        FontSize="17"
                        FontWeight="Bold"
                        Foreground="#333333"
                        Text="{Binding Source={x:Static local:ResourceHelper.Instance}, Path=WindowTitle}" />
                    <TextBlock
                        Margin="0,5,0,0"
                        HorizontalAlignment="Center"
                        FontSize="14"
                        Foreground="#666666" />
                </StackPanel>

                <!--  用户名输入框  -->
                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                    <TextBlock
                        Margin="0,0,0,5"
                        FontSize="14"
                        Foreground="#666666"
                        Text="{Binding Source={x:Static local:ResourceHelper.Instance}, Path=WindowLoginUser}" />
                    <TextBox
                        x:Name="login_Name"
                        Style="{StaticResource InputTextBoxStyle}"
                        Tag="{Binding Source={x:Static local:ResourceHelper.Instance}, Path=WindowLoginUserTitle}" />
                </StackPanel>

                <!--  密码输入框  -->
                <StackPanel Grid.Row="2" Margin="0,0,0,15">
                    <TextBlock
                        Margin="0,0,0,5"
                        FontSize="14"
                        Foreground="#666666"
                        Text="{Binding Source={x:Static local:ResourceHelper.Instance}, Path=WindowLoginPass}" />
                    <PasswordBox
                        x:Name="login_pass"
                        Style="{StaticResource InputTextBoxStyle}"
                        Tag="{Binding Source={x:Static local:ResourceHelper.Instance}, Path=WindowLoginPassTitle}" />
                </StackPanel>



                <!--  并排按钮（登录/退出）  -->
                <StackPanel
                    Grid.Row="4"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
                    <Button
                        Click="LoginButton_Click"
                        Content="{Binding Source={x:Static local:ResourceHelper.Instance}, Path=WindowLoginOK}"
                        Style="{StaticResource AccentButtonStyle}" />
                    <Button
                        Click="ExitButton_Click"
                        Content="{Binding Source={x:Static local:ResourceHelper.Instance}, Path=WindowLoginCancel}"
                        Style="{StaticResource ExitButtonStyle}" />
                </StackPanel>

                <!--  版本信息  -->
                <TextBlock
                    Grid.Row="5"
                    Margin="0,15,0,0"
                    HorizontalAlignment="Center"
                    FontSize="12"
                    Foreground="#AAAAAA"
                    Text="{Binding Source={x:Static local:ResourceHelper.Instance}, Path=WindowVersion}" />
            </Grid>
            <Canvas
                x:Name="NotificationHost"
                Grid.ColumnSpan="3"
                Width="350"
                Height="200"
                Margin="0,20,-100,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Panel.ZIndex="10000" />
        </Grid>
    </Border>

    <!--  窗口加载动画  -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <!--  淡入动画  -->
                    <DoubleAnimation
                        Storyboard.TargetName="LoginPanel"
                        Storyboard.TargetProperty="Opacity"
                        From="0"
                        To="1"
                        Duration="0:0:0.5" />
                    <!--  上移动画  -->
                    <DoubleAnimation
                        EasingFunction="{StaticResource CubicEaseOut}"
                        Storyboard.TargetName="LoginPanel"
                        Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                        From="20"
                        To="0"
                        Duration="0:0:0.5" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>